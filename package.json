{
  "name": "edu-platform",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build-frontend": "cd edu-frontend && npm install && npm run build",
    "deploy": "npm run build-frontend && cd backend && npm start"
  },
  "dependencies": {
    "openai": "^6.3.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.14"
  }
}
